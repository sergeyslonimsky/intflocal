linters-settings:
  custom:
    intflocal:
      type: "module"
      description: "Checks that struct interface dependencies are defined locally as private interfaces"
      settings:
        packages:
          - "./..."
        excludePackages: []
        excludeTypes: []

linters:
  disable-all: true
  enable:
    - intflocal
